{
  "installCommand": "NPM_CONFIG_PRODUCTION=false npm install --include=dev",
  "buildCommand": "npm run build --workspace=@arena/web",
  "outputDirectory": "apps/web/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
